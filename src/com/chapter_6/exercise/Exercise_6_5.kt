package com.chapter_6.exercise

/*
    Option을 반환하는 함수를 매핑하는 방법이 필요한 것처럼
    Option 타입의 기본 값을 받는 getOrElse가 필요할 수도 있다.
    시그니쳐가 다음과 같은 orElse 함수를 작성하라.
    fun orElse(default: () -> Option<A>): Option<A>
 */